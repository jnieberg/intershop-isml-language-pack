# Intershop ISML Language Pack
Simple syntax highlighting tool for Intershop ISML templates. Supports highlighting for:
- isml tags (`<isprint value="Hi">`)
- variables (`#variable#`)
- jsp (`<%script%>`)
- comments (`<!---comment--->` and `<iscomment>hello</iscomment>`)